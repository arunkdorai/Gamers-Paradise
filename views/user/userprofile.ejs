<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Gamers Paradise</title>
    <link rel="stylesheet" href="/css/style.css" />
    <!-- Bootstrap CSS -->
    <link
      href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
      rel="stylesheet"
    />

    <!-- Font Awesome CSS -->
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/css/style.css" />
    <style>
      body {
        background-color: #552FA8;
        font-family: 'Arial', sans-serif;
      }
      .profile-box {
        background-color: #ffffff;
        border-radius: 15px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        transition: all 0.3s ease;
      }
      .profile-box:hover {
        transform: translateY(-5px);
        box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
      }
      h1, h2 {
        color: #4a4a4a;
        border-bottom: 2px solid #3498db;
        padding-bottom: 10px;
      }
      .profile-details p, .profile-address p {
        color: #666;
        line-height: 1.6;
      }
      .btn-primary {
        background-color: #3498db;
        border-color: #3498db;
      }
      .btn-primary:hover {
        background-color: #2980b9;
        border-color: #2980b9;
      }
      .btn-secondary {
        background-color: #2ecc71;
        border-color: #2ecc71;
      }
      .btn-secondary:hover {
        background-color: #27ae60;
        border-color: #27ae60;
      }
      .btn-danger {
        background-color: #e74c3c;
        border-color: #e74c3c;
      }
      .btn-danger:hover {
        background-color: #c0392b;
        border-color: #c0392b;
      }
      .btn-warning {
        background-color: #f39c12;
        border-color: #f39c12;
      }
      .btn-warning:hover {
        background-color: #d35400;
        border-color: #d35400;
      }
      .btn-success {
        background-color: #1abc9c;
        border-color: #1abc9c;
      }
      .btn-success:hover {
        background-color: #16a085;
        border-color: #16a085;
      }
      .addcard {
        border: none;
        border-left: 5px solid #3498db;
        transition: all 0.3s ease;
      }
      .addcard:hover {
        background-color: #f7f7f7;
      }
    </style>
        <style>
          :root {
      --primary-color: #552FA8;
      --secondary-color: #2eaccc;
      --accent-color: #e74c3c;
      --text-color: #333;
      --light-bg: #f8f9fa;
    }
    
    body {
      font-family: 'Arial', sans-serif;
      color: var(--text-color);
    }
    
    /* Navbar styles */
    .navbar {
      background-color: var(--primary-color) !important;
      padding: 15px 0;
    }
    
    .navbar-brand img {
      height: 80px;
    }
    
    .navbar-nav .nav-link {
      color: white !important;
      font-weight: bold;
      margin: 0 10px;
      transition: color 0.3s ease;
    }
    
    .navbar-nav .nav-link:hover {
      color: var(--secondary-color) !important;
    }
    
    .search-form {
      color: #f8f9fa;
      position: relative;
    }
    
    .search-input {
      border-radius: 20px;
      padding: 20px;
      background-color: #f8f9fa;
    }
    
    .search-icon {
      position: absolute;
      right: 10px;
      top: 50%;
      transform: translateY(-50%);
      background: none;
      border: none;
      color: var(--light-bg);
    }
    
    @media (min-width: 576px) {
        .mr-sm-2, .mx-sm-2 {
            margin-right: -1.5rem !important;
        }
    }
    
    .wishlist-icon, .cart-icon {
      color: white;
      font-size: 1.2em;
      margin: 0 10px;
      transition: color 0.3s ease;
    }
    
    .wishlist-icon:hover, .cart-icon:hover {
      color: var(--secondary-color);
    }
    
    .user-info .nav-link {
      color: white !important;
    }
    
    .dropdown-menu {
      background-color: var(--light-bg);
    }
    
    .dropdown-item {
      color: var(--text-color);
      transition: background-color 0.3s ease;
    }
    
    .dropdown-item:hover {
      background-color: var(--primary-color);
      color: white;
    }
    
        </style>
  </head>
  <body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <!-- Logo -->
      <a class="navbar-brand" href="/">
        <img src="/images/logo.png" alt="Logo" />
      </a>

      <!-- Nav Links -->
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item active">
            <a class="nav-link" href="/">HOME</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/console">CONSOLE</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/games">GAMES</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/computer">COMPUTER</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/shop">SHOP</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/about">ABOUT US</a>
          </li>
        </ul>
      </div>

      <% if (fullName) { %>
        <div class="user-info dropdown">
          <a href="#" class="nav-link dropdown-toggle" id="userDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <%= fullName %>
          </a>
          <div class="dropdown-menu" aria-labelledby="userDropdown">
            <a class="dropdown-item" href="/profile">Profile</a>
            <div class="dropdown-divider"></div>
            <a class="dropdown-item" href="/logout">Logout</a>
          </div>
        </div>
        <% } %>

      <!-- Hamburger Menu Toggle Button -->
      <button
        class="navbar-toggler ml-auto"
        type="button"
        data-toggle="collapse"
        data-target="#navbarNav"
        aria-controls="navbarNav"
        aria-expanded="false"
        aria-label="Toggle navigation"
      >
        <span class="navbar-toggler-icon">&#9776;</span>
      </button>
    </nav>

    <main class="container mt-5" style="max-width: 800px; padding: 20px;">
      <div class="profile-box p-4 mb-4">
        <h1 class="mb-4">User Profile</h1>
        <div class="profile-details mb-4">
          <!-- Display user details here -->
          <p>Name: <%= user.fullname %></p>
          <p>Email: <%= user.email %></p>
          <p>Phone Number: <%= user.phone %></p>
          <!-- Added phone number field -->
        </div>
      </div>
      <div class="profile-box p-4 mb-4">
        <h2 class="mb-4">Address</h2>
        <div class="profile-address mb-4">
          <!-- Display user addresses -->
          <% if (user.addresses.length > 0) { %>
            <% user.addresses.forEach((addr) => { %>
              <div class="card addcard mb-3" style="border: 2.5px solid #3498db!important;">
                <div class="card-body">
                  <div class="row">
                    <div class="col-md-12">
                      <h5 class="card-title" style="color: #3498db;"><i class="fas fa-map-marker-alt"></i> Address</h5>
                      <p class="card-text"><%= addr.address %></p>
                      <p class="card-text"><%= addr.addressline2 %></p>
                      <p class="card-text"><%= addr.city %>, <%= addr.state %></p>
                      <p class="card-text"><strong>PIN CODE:</strong> <%= addr.pincode %></p>
                    </div>
                  </div>
                </div>
              </div>
            <% }); %>
          <% } else { %>
            <p>No address found. <a href="/add-address" class="btn btn-sm btn-primary"><i class="fas fa-plus-circle"></i> Add Address</a></p>
          <% } %>
        </div>
        <!-- Buttons for managing addresses -->
        <div class="btn-group">
          <!-- Add new address -->
          <a href="/add-address" class="btn btn-primary"><i class="fas fa-plus-circle"></i> Add Address</a>
          <a href="/view-addresses" class="btn btn-secondary"><i class="fas fa-eye"></i> View Addresses</a>
        </div>
      </div>
      <div class="profile-box p-4 mb-4">
        <div class="profile-orders mb-4">
          <!-- Display user orders here -->
          <h2 class="mb-4">Details</h2>
          <!-- Add logic to display user orders -->
        </div>
        <div class="profile-options">
          <!-- Edit Profile -->
          <a href="/edit-profile" class="btn btn-primary mr-2"><i class="fas fa-user-edit"></i> Edit Profile</a>
          <a href="/orders" class="btn btn-danger mr-2"><i class="fas fa-shopping-bag"></i> Orders</a>
          <a href="/change-password" class="btn btn-warning"><i class="fas fa-key"></i> Change Password</a>
          <a href="/wallet" class="btn btn-success"><i class="fas fa-wallet"></i> Wallet</a>
        </div>
      </div>
    </main>

    <script src="https://kit.fontawesome.com/a076d05399.js"></script>
  </body>
</html>
